# EXAMPLE ONLY: enter actual values in Streamlit Cloud > Settings > Secrets.
# Never commit .streamlit/secrets.toml or real passwords/keys.
[access]
password = "REPLACE_WITH_A_LONG_UNIQUE_PASSWORD"

# Omit this entire section to use temporary SQLite storage.
[supabase]
url = "https://YOUR_PROJECT.supabase.co"
key = "REPLACE_WITH_SERVER_SECRET_KEY"
